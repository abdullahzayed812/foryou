# Edge Nginx — see foryou.citymarket.tech.conf in this directory for the
# full explanation. This one proxies to the `api` container and adds the
# websocket upgrade headers Socket.IO needs (map defined in upgrade-map.conf).

server {
  listen 80;
  server_name api.foryou.citymarket.tech;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 301 https://$host$request_uri;
  }
}

server {
  listen 443 ssl;
  http2 on;
  server_name api.foryou.citymarket.tech;

  ssl_certificate /etc/letsencrypt/live/api.foryou.citymarket.tech/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/api.foryou.citymarket.tech/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_ciphers HIGH:!aNULL:!MD5;
  ssl_prefer_server_ciphers off;

  location / {
    proxy_pass http://api:4000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}
